# .github/workflows/publish.yml

name: Quarto Publish

on:
  push:
    branches: main

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    # Using a login shell can help Conda initialize correctly
    defaults:
      run:
        shell: bash -l {0}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Conda environment
        id: setup-conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          environment-file: environment.yml
          activate-environment: quarto-env
          use-mamba: true

      # --- THIS IS THE CRITICAL NEW STEP ---
      - name: Add Conda environment to PATH
        run: |
          echo "Prepending Conda bin to GITHUB_PATH"
          echo "${{ steps.setup-conda.outputs.conda-prefix }}/bin" >> $GITHUB_PATH
        
      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Render and Publish
        uses: quarto-dev/quarto-actions/publish@v2
        with:
          target: gh-pages
        # We still set QUARTO_PYTHON as a backup, but the PATH change should be the primary fix
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          QUARTO_PYTHON: ${{ steps.setup-conda.outputs.conda-prefix }}/bin/python
